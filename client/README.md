# FlashERP Client

This is the Next.js frontend for the FlashERP platform.

## Development

To run the entire platform (client + server), please use the root directory commands:
```bash
cd ..
npm run dev
```

To run **only** the client independently:
```bash
npm run dev
```

For more details, see the [Root README](../README.md).
