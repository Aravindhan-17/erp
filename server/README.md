# FlashERP Server

This is the NestJS backend for the FlashERP platform.

## Development

To run the entire platform (client + server), please use the root directory commands:
```bash
cd ..
npm run dev
```

To run **only** the server independently:
```bash
npm run start:dev
```

For more details, see the [Root README](../README.md).
